services:
  - type: web
    name: best-idea-app
    runtime: docker
    plan: free
    envVars:
      - key: REDIS_URL
        fromService:
          type: redis
          name: best-idea-redis
          property: connectionString

databases:
  - name: best-idea-redis
    type: redis
    plan: free